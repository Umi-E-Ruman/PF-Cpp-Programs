#include <iostream>
using namespace std;

int main() {
    char ch;
    cout << "Enter a character: ";
    cin >> ch;

    if (ch == 'a')
	 cout << ch << " is a vowel." << endl;
    if (ch == 'e')
	 cout << ch << " is a vowel." << endl;
    if (ch == 'i')
	 cout << ch << " is a vowel." << endl;
    if (ch == 'o') 
	cout << ch << " is a vowel." << endl;
    if (ch == 'u') 
	cout << ch << " is a vowel." << endl;
    if (ch == 'A')
	 cout << ch << " is a vowel." << endl;
    if (ch == 'E')
	 cout << ch << " is a vowel." << endl;
    if (ch == 'I')
	 cout << ch << " is a vowel." << endl;
    if (ch == 'O') 
	cout << ch << " is a vowel." << endl;
    if (ch == 'U') 
	cout << ch << " is a vowel." << endl;

    if (ch == 'b')
	 cout << ch << " is a consonant." << endl;
    if (ch == 'c') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'd') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'f')
	 cout << ch << " is a consonant." << endl;
    if (ch == 'g')
	 cout << ch << " is a consonant." << endl;
    if (ch == 'h') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'j') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'k') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'l') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'm') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'n') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'p')
	 cout << ch << " is a consonant." << endl;
    if (ch == 'q') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'r')
	 cout << ch << " is a consonant." << endl;
    if (ch == 's')
	 cout << ch << " is a consonant." << endl;
    if (ch == 't')
	 cout << ch << " is a consonant." << endl;
    if (ch == 'v') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'w') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'x') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'y')
	 cout << ch << " is a consonant." << endl;
    if (ch == 'z') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'B')
	 cout << ch << " is a consonant." << endl;
    if (ch == 'C')
	 cout << ch << " is a consonant." << endl;
    if (ch == 'D')
	 cout << ch << " is a consonant." << endl;
    if (ch == 'F') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'G')
	 cout << ch << " is a consonant." << endl;
    if (ch == 'H')
	 cout << ch << " is a consonant." << endl;
    if (ch == 'J') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'K') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'L') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'M') 
	cout << ch << " is a consonant." << endl;
    if (ch == 'N')
	 cout << ch << " is a consonant." << endl;
    if (ch == 'P')
	 cout << ch << " is a consonant." << endl;
    if (ch == 'Q')
	 cout << ch << " is a consonant." << endl;
    if (ch == 'R')
	 cout << ch << " is a consonant." << endl;
    if (ch == 'S')
	 cout << ch << " is a consonant." << endl;
    if (ch == 'T') 
	cout << ch << " is a consonant." << endl;
if (ch == 'V')
 cout << ch << " is a consonant." << endl;
if (ch == 'W') 
cout << ch << " is a consonant." << endl;
if (ch == 'X')
 cout << ch << " is a consonant." << endl;
if (ch == 'Y') 
cout << ch << " is a consonant." << endl;
if (ch == 'Z') 
cout << ch << " is a consonant." << endl;

if (ch == '0') 
cout << ch << " is a digit." << endl;
if (ch == '1') 
cout << ch << " is a digit." << endl;
if (ch == '2') 
cout << ch << " is a digit." << endl;
if (ch == '3') 
cout << ch << " is a digit." << endl;
if (ch == '4') 
cout << ch << " is a digit." << endl;
if (ch == '5') 
cout << ch << " is a digit." << endl;
if (ch == '6') 
cout << ch << " is a digit." << endl;
if (ch == '7')
 cout << ch << " is a digit." << endl;
if (ch == '8') 
cout << ch << " is a digit." << endl;
if (ch == '9') 
cout << ch << " is a digit." << endl;

return 0;

}

